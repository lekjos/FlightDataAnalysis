jupyter==1.0.0          # Pretty notebook that runs code
python-dotenv==0.19.2   # Hides keys in .env file instead of repo
psycopg2==2.9.3         # Postgres python driver (used DBConn class)
pandas==1.3.5           # Used for EDA and upload
sqlalchemy==1.4.31      # Load data with Pandas
numpy==1.22.1            
chardet==4.0.0          # Detects encoding, used in EDA